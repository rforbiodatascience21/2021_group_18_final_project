# Clear workspace ---------------------------------------------------------
rm(list = ls())

# Load data ---------------------------------------------------------------
#alon_clean <- read_tsv(file = "data/alon_clean.tsv.gz")
joined_data
cancer_data

# Heat Map ---------------------------------------------------------------

